{
  "name": "@datewise/web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run build --workspace @datewise/shared && next dev",
    "build": "npm run build --workspace @datewise/shared && next build",
    "start": "next start",
    "typecheck": "npm run build --workspace @datewise/shared && tsc --noEmit"
  },
  "dependencies": {
    "@datewise/shared": "1.0.0",
    "next": "^15.5.12",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "typescript": "^5.6.3"
  }
}
